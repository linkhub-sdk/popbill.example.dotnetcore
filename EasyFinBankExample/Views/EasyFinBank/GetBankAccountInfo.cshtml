@{
	ViewBag.Title = "GetBankAccountInfo (계좌정보 확인)";
}

<div id="content">
	<p class="heading1">Response</p>
	<br />
	<fieldset class="fieldset1">
		<legend>@ViewData["Title"]</legend>
		
			<fieldset class="fieldset2">
				<legend>계좌 정보</legend>
				<ul>
					<li>bankCode (은행코드) : @Model.bankCode</li>
					<li>accountNumber (계좌번호) : @Model.accountNumber</li>
					<li>accountName (계좌별칭) : @Model.accountName</li>
					<li>accountType (계좌유형) : @Model.accountType</li>
					<li>state (정액제 상태) : @Model.state</li>
					<li>regDT (등록일시) : @Model.regDT</li>
					<li>memo (메모) : @Model.memo</li>

					<li>contractDT (정액제 서비스 시작일시) : @Model.contractDT</li>
					<li>useEndDate (정액제 서비스 종료일) : @Model.useEndDate</li>
					<li>baseDate (자동연장 결제일) : @Model.baseDate</li>
					<li>contractState (정액제 서비스 상태) : @Model.contractState</li>
					<li>closeRequestYN (정액제 서비스 해지신청 여부) : @Model.closeRequestYN</li>
					<li>useRestrictYN (정액제 서비스 사용제한 여부) : @Model.useRestrictYN</li>
					<li>closeOnExpired (정액제 서비스 만료 시 해지 여부) : @Model.closeOnExpired</li>
					<li>unPaidYN (미수금 보유 여부) : @Model.unPaidYN</li>
				</ul>
			</fieldset>
		</fieldset>
</div>
