@{
	ViewBag.Title = "ListBankAccount (계좌 목록 확인)";
}

<div id="content">
	<p class="heading1">Response</p>
	<br />
	<fieldset class="fieldset1">
		<legend>@ViewData["Title"]</legend>
		@{
			int i = 0;
			foreach (var list in Model)
			{
				i++;
				<fieldset class="fieldset2">
					<legend>계좌 정보 [ @i / @Model.Count ]</legend>
					<ul>
						<li>bankCode (기관코드) : @list.bankCode</li>
						<li>accountNumber (계좌번호) : @list.accountNumber</li>
						<li>accountName (계좌별칭) : @list.accountName</li>
						<li>accountType (계좌유형) : @list.accountType</li>
						<li>state (정액제 상태) : @list.state</li>
						<li>regDT (등록일시) : @list.regDT</li>
						<li>memo (메모) : @list.memo</li>

						<li>contractDT (정액제 서비스 시작일시) : @list.contractDT</li>
						<li>useEndDate (정액제 서비스 종료일) : @list.useEndDate</li>
						<li>baseDate (자동연장 결제일) : @list.baseDate</li>
						<li>contractState (정액제 서비스 상태) : @list.contractState</li>
						<li>closeRequestYN (정액제 서비스 해지신청 여부) : @list.closeRequestYN</li>
						<li>useRestrictYN (정액제 서비스 사용제한 여부) : @list.useRestrictYN</li>
						<li>closeOnExpired (정액제 서비스 만료 시 해지 여부) : @list.closeOnExpired</li>
						<li>unPaidYN (미수금 보유 여부) : @list.unPaidYN</li>


					</ul>
				</fieldset>
			}
		}
	</fieldset>
</div>