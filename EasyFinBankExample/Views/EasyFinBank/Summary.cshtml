@{
    ViewBag.Title = "Summary (수집 결과 요약정보 조회)";
}

<div id="content">
    <p class="heading1">Response</p>
    <br />
    <fieldset class="fieldset1">
        <legend>@ViewData["Title"]</legend>
        <ul>
            <li> count (수집 결과 건수) : @Model.count</li>
            <li> cntAccIn (입금거래 건수) : @Model.cntAccIn</li>
            <li> cntAccOut (출금거래 건수) : @Model.cntAccOut</li>
            <li> totalAccIn (입금액 합계) : @Model.totalAccIn</li>
            <li> totalAccOut (출금액 합계) : @Model.totalAccOut</li>
        </ul>
    </fieldset>
</div>