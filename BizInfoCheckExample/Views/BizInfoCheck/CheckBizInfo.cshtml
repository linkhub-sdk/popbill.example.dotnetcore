@{
    ViewBag.Title = "CheckBizInfo (기업정보조회)";
}

<div id="content">
    <p class="heading1">Response</p>
    <br/>
    <fieldset class="fieldset1">
        <legend>@ViewData["Title"]</legend>
        <br/>
        <ul>
            <li>corpNum (사업자번호) : @Model.corpNum</li>
            <li>companyRegNum (법인번호) : @Model.companyRegNum</li>
            <li>checkDT (확인일시) : @Model.checkDT</li>
            <li>corpName (상호) : @Model.corpName</li>
            <li>corpCode (기업형태코드) : @Model.corpCode</li>
            <li>corpScaleCode (기업규모코드) : @Model.corpScaleCode</li>
            <li>personCorpCode (개인법인코드) : @Model.personCorpCode</li>
            <li>headOfficeCode (본점지점코드) : @Model.headOfficeCode</li>
            <li>industryCode (산업코드) : @Model.industryCode</li>
            <li>establishCode (설립구분코드) : @Model.establishCode</li>
            <li>establishDate (설립일자) : @Model.establishDate</li>
            <li>CEOName (대표자명) : @Model.ceoname</li>
            <li>workPlaceCode (사업장구분코드) : @Model.workPlaceCode</li>
            <li>addrCode (주소구분코드) : @Model.addrCode</li>
            <li>zipCode (우편번호) : @Model.zipCode</li>
            <li>addr (주소) : @Model.addr</li>
            <li>addrDetail (상세주소) : @Model.addrDetail</li>
            <li>enAddr (영문주소) : @Model.enAddr</li>
            <li>bizClass (업종) : @Model.bizClass</li>
            <li>bizType (업태) : @Model.bizType</li>
            <li>result (결과코드) : @Model.result</li>
            <li>resultMessage (결과메시지) : @Model.resultMessage</li>
            <li>closeDownTaxType (사업자과세유형) : @Model.closeDownTaxType</li>
            <li>closeDownTaxTypeDate (과세유형전환일자) : @Model.closeDownTaxTypeDate</li>
            <li>closeDownState (휴폐업상태) : @Model.closeDownState</li>
            <li>closeDownStateDate (휴폐업일자) : @Model.closeDownStateDate</li>
        </ul>
    </fieldset>
</div>
